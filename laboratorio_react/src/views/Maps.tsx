import {useState} from 'react'; import type {Simulation} from '../types'; import {ForestMap2D} from '../components/ForestMap2D'; import {ForestScene} from '../three/ForestScene';
export function MapsView({result}:{result:Simulation|null}){const [compare,setCompare]=useState(false);if(!result)return <div className="panel empty">Ejecuta una simulación para cargar las capas 2D y la escena 3D.</div>;return <div className="maps"><div className="panel"><h2>Mapa 2D analítico</h2><p className="muted">Biomasa predicha, riesgo de incendio y pérdida GFW con capas alternables.</p><ForestMap2D data={result.trees}/></div><div className="panel"><div className="rowTitle"><div><h2>Bosque 3D realista</h2><p className="muted">700 árboles instanciados por GPU: tronco + copa procedural.</p></div><label className="switch"><input type="checkbox" checked={compare} onChange={e=>setCompare(e.target.checked)}/> Antes / después</label></div><ForestScene data={result.trees} compare={compare}/></div></div>}
