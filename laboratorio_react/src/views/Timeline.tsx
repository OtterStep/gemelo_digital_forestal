import type {Simulation} from '../types'; import {TimeSeries} from '../components/TimeSeries';
export function TimelineView({result}:{result:Simulation|null}){return <div className="panel"><h2>Serie temporal</h2><p className="muted">Evolución mensual sintética de biomasa y riesgo de incendio.</p>{result?<><TimeSeries rows={result.temporal}/><div className="tableWrap"><table><thead><tr><th>Periodo</th><th>Biomasa base</th><th>Biomasa escenario</th><th>Riesgo base</th><th>Riesgo escenario</th></tr></thead><tbody>{result.temporal.map(x=><tr key={x.period}><td>{x.period}</td><td>{x.baseline.toFixed(1)}</td><td>{x.scenario.toFixed(1)}</td><td>{(x.riesgo_base*100).toFixed(1)}%</td><td>{(x.riesgo_escenario*100).toFixed(1)}%</td></tr>)}</tbody></table></div></>:<div className="empty">No hay simulación cargada.</div>}</div>}
