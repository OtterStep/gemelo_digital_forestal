import {useEffect,useState} from 'react'; import {getModel} from './services/api'; import {useSimulacion} from './hooks/useSimulacion'; import {useExplicacionIA} from './hooks/useExplicacionIA'; import type {Scenario,Model} from './types'; import {ScenarioView} from './views/Scenario'; import {MapsView} from './views/Maps'; import {TimelineView} from './views/Timeline'; import {ExportView} from './views/Export';
const initial:Scenario={clareo_pct:10,quema_pct:5,restauracion_plantas_ha:80,manejo_combustibles:20,fmc:55,lai:4,ndvi:.72,precipitacion_mm:180,temperatura_c:27,year:2026,month:9};
export default function App(){const [model,setModel]=useState<Model|null>(null);const [tab,setTab]=useState('Escenario');const [s,setS]=useState(initial);const {loading,result,run}=useSimulacion();const ia=useExplicacionIA();const explicar=()=>{if(result)ia.run(s,result)};useEffect(()=>{getModel().then(setModel);},[]);const tabs=['Escenario','Mapas 2D/3D','Serie temporal','Exportar reporte'];const iaProps={configurado:ia.configurado,explicacion:ia.explicacion,cargando:ia.cargando,error:ia.error,onExplicar:explicar};return <main><header><div><small>LABORATORIO DEL MEJOR MODELO · GEMELO DIGITAL FORESTAL</small><h1>{model?.nombre||'Cargando modelo…'}</h1><p>R² {(model?.metricas.r2??0).toFixed(2)} · RMSE {(model?.metricas.rmse??0).toFixed(1)} · MAE {(model?.metricas.mae??0).toFixed(1)} · {model?.source_version||'synthetic_test'}</p></div><div className="status"><span>●</span> INFERENCIA</div></header><nav>{tabs.map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav>{tab==='Escenario'&&<ScenarioView s={s} setS={setS} result={result} run={()=>run(s)} loading={loading} ia={iaProps}/>} {tab==='Mapas 2D/3D'&&<MapsView result={result}/>} {tab==='Serie temporal'&&<TimelineView result={result}/>} {tab==='Exportar reporte'&&<ExportView s={s} result={result} ia={iaProps}/>}<footer>BR-Sa1 como referencia geográfica · inferencia con modelos entrenados (fuente {model?.source_version||'—'}) · no usar para decisiones operativas reales</footer></main>}
