import type {Simulation} from '../types';
export function TimeSeries({rows}:{rows:Simulation['temporal']}){const max=Math.max(...rows.map(x=>Math.max(x.baseline,x.scenario)));const points=(key:'baseline'|'scenario')=>rows.map((x,i)=>`${(i/(rows.length-1))*100},${95-(x[key]/max)*80}`).join(' ');return <div className="chart"><svg viewBox="0 0 100 100" preserveAspectRatio="none"><polyline points={points('baseline')} fill="none" stroke="currentColor" strokeWidth="1.2"/><polyline points={points('scenario')} fill="none" stroke="#39784a" strokeWidth="1.5"/></svg><div className="chartLegend"><span>— Línea base</span><span className="green">— Escenario</span></div></div>}
